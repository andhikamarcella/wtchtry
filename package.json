{
  "name": "tiktok-history-viewer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html && cp -R src dist/src"
  }
}
